/**
 * @module
 * [EN] Node.js filesystem adapter entry. The implementation lives in `src/barrits/sdk/adapters.ts` and is shared
 * with Bun and the bundler plugins.
 * [ES] Entrada del adaptador de filesystem de Node.js. La implementación vive en `src/barrits/sdk/adapters.ts` y se
 * comparte con Bun y los plugins de bundler.
 */
export { createNodeFileSystemAdapter } from "../../src/barrits/sdk/adapters";
